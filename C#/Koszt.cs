
internal class Koszt
{
    decimal Calculate(decimal netAmount, object koszt)
    {
        object = int(5, 12);
    }
    public static int choice { get; private set; }

    public static void Main(string[] args)
    {
        Dictionary<string, string> Wszystko =
    new Dictionary<string, string>();
        Wszystko.Add("txt", "notepad.exe");
        Wszystko.Add("bmp", "paint.exe");
        Wszystko.Add("dib", "paint.exe");
        Wszystko.Add("rtf", "wordpad.exe");




        switch (choice)
        {
            case 1:
                Console.Write("jest 50zł ");
                decimal netAmount = decimal.Parse(Console.ReadLine());
                decimal grossAmount = Calculate(netAmount, Koszt);
                Console.WriteLine($" 5zł");
                break;
                
}
}
